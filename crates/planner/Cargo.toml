[package]
name = "itrtg-planner"
version = "0.1.0"
edition = "2024"

[dependencies]
itrtg-models = { path = "../models" }
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"

[dev-dependencies]
anyhow = "1"
